|***|***|***|***|***|***|***|***|***|***|***|***|***|***|***|...
|********|********|********|********|********|********|********|

Use Bresenham-esque incrementation
s=source_width
w=dest width (which must be < source width)
a=0;
while(a<source_width)
{
	pixel_factor=1.0;
	next_pixel();
	a+=dest_width;
}
// Now the accumulator is > source_width, so we subtract source_width.
a-=source_width;
// a / source_width is now the pixel factor for the current pixel,
// and (1-a/source_width) is the pixel factor the next pixel.
pixel_factor=a;
store_pixel();
next_pixel();
pixel_factor=1-a;
next_pixel();

